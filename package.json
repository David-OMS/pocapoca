{
  "name": "birthday-site",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "node scripts/sync-photo.mjs && npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "build": "node scripts/sync-photo.mjs && npm ci --include=dev --prefix client && npm run build --prefix client && node scripts/sync-photo.mjs && npm ci --prefix server",
    "start": "node scripts/start-prod.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
